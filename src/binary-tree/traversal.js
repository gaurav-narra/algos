const inOrder = (node, arr = []) => {
}

const preOrder = (node, arr = []) => {
}

const postOrder = (node, arr = []) => {
}

const levelOrder = (node) => {
}

module.exports = {
  inOrder,
  preOrder,
  postOrder,
  levelOrder
}